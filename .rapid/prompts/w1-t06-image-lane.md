W1 supervisor for T-06 image-lane-generalize. Read .rapid/prompts/_w1_common.md, 04-spec/spec.md S3 (S3.3 PANEL caps: K=8/target, 24/tenant, 80 global, $5/day/tenant realtime ceiling, fail-loud warm script; S3.4 the S02 realtime beat), .rapid/TASKS.json T-06, pipeline/personalization/image_gen.py (+_call_image_api cost path), prebuild.py, rules/{gauntlet,planet}_prebuild.yaml + *_image.yaml, scripts/warm_hero_cache.py, rules/api_pricing.yaml.
Deliver: (1) surfaces driven by registry image_targets (sections.list_image_targets) so targets beyond hero/og resolve; (2) cap enforcement in prebuild.py + warm_hero_cache.py --check mode that FAILS (exit 1) if manifest exceeds K=8/target, 24/tenant, 80 global — wire into deploy/railway.sh after its pytest line; (3) per-tenant realtime daily ceiling ($5 default, env-overridable APT_REALTIME_CEILING_USD) checked via api_costs before a realtime generate; past ceiling → neutral prebuilt/gradient fallback (logged, not silent); (4) tests/test_image_caps.py: cap violations rejected, ceiling fallback engages, cost-logged realtime path (monkeypatch httpx like test_wf_design_matrix does), offline deterministic fallback. Do NOT regenerate any images; no API calls in tests.
