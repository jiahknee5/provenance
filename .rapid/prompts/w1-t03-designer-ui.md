W1 supervisor for T-03 designer-ui. Read .rapid/prompts/_w1_common.md, 04-spec/contracts/designer-dom.md (THE sd-* testid contract), 04-spec/spec.md S5, .rapid/TASKS.json T-03, pipeline/personalization/sections.py + rules/*_sections.yaml (T-01 output — the registry you render), pipeline/personalization/gauntlet_dev_business.py (_copy_cards :600, _image_surface_card :696, build_business_console_view :921) and gauntlet_dev_business.html (staged drawer :612-798), planet parallels.
Rework the marketer consoles (gauntlet+planet) into the per-section designer: one sd-section-{id} card per registry section binding its text+image targets; Q1-Q10 controls per designer-dom.md (read-only values from registry+page state this wave; edits stage YAML diffs to rules/<tenant>_sections.yaml through the existing drawer — extend patchFor with a "sections" kind); dev/audit toggle (sd-devtoggle) folding the engineer console's trace/audit sections in collapsed form. Keep ALL existing console tests green (they assert marketer-first ordering, hub-grid, staged drawer, channel strip etc. — read tests/test_demo_nav.py console tests + tests/test_apt_dev_hub.py first). Add tests/test_designer_ui.py asserting the DOM contract per section.
