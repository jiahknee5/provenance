W4 supervisor for T-09 single-design-retire-legacy. Read .rapid/prompts/_w1_common.md (same rules; this task runs in the MAIN tree serially — no parallel siblings — but still commit-per-route keep-or-revert), 04-spec/spec.md S8+S0, PRD R38/R39 (docs/01-intake/PRD-SECTION-DESIGNER.md), the audit inventory in docs/05-build/DEMO-NAV-VISION-GAP.md §1.C (the ~30 legacy routes).
Protocol PER ROUTE-GROUP (small batches: e.g. /demo*, then /workspace+/records, then /inspector+/graph+/agent, ...): remove route registrations + templates; fold still-needed content per S8 mapping (assurance/optimizer/drift→S4 panels exist via T-04; inspector/graph→per-section obs; enrichment-catalog→Data panel note); update ONLY tests that assert the removed routes (test updates are architecture-updates, never weakenings — the 5 property tests + WF-DESIGN harness are UNTOUCHABLE; if a property test exercises a legacy route (e.g. /site/<token>, /demo pipeline surfaces), KEEP that route working — property tests win over retirement, log the exception); run the FULL gate incl. tests/test_full_suite.py if present; commit on green, revert the batch on red. Remove base.html/_nav.html only when zero remaining references. End with the S0.1 cohesion sweep (zero orphan routes vs sidebar map; no legacy chrome served) written to .rapid/COHESION.md.
