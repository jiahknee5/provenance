W3 supervisor for T-08 wf-design-matrix. Read .rapid/prompts/_w1_common.md (same rules), docs/04-workflow/SECTION-DESIGNER-EXAMPLES.md §3 (all 30 rows verbatim), tests/test_wf_design_matrix.py (LOCKED — it pins the demo_scenarios.yaml schema your scenarios must satisfy and the per-row assertions), .rapid/EVAL/MANIFEST.json, rules/demo_scenarios.yaml (existing 13 WF-DEMO scenarios — APPEND, never clobber), sections.py registries, decision_pool, skyfi tenant (all merged).
Deliver: the 30 scenarios (G01-G10, P01-P10, S01-S10) appended to rules/demo_scenarios.yaml exactly matching the locked harness schema + matrix rows (channel/element/mode/workflow/strategy/guardrail), wiring each to real landing URLs + console deep links for its tenant. You may fan out implementor subagents in batches of ≤4 rows-per-agent if useful. VERIFY: pytest tests/test_wf_design_matrix.py -q must show ALL 30 rows ACTIVE and GREEN (0 skipped) + the 4 invariants + full deploy gate green. This is the wave where the harness fully activates — if a row can't go green, the BUILD is wrong, not the test (Art VII): report the gap, don't touch the harness.
