W2 supervisor for T-05 generative-text. Read .rapid/prompts/_w1_common.md (same rules), 04-spec/spec.md S2, 04-spec/contracts/registry-schema.md (generative target fields: prompt+gate required) + decision-pool.md, .rapid/TASKS.json T-05, THEN merged state: pipeline/personalization/decision_pool.py (T-02), sections.py + rules/*_sections.yaml, pipeline/personalization/creative.py (ai_copy + verify_copy), pipeline/generation/variants.py (build_action_pool), tests/test_wf_design_matrix.py gen-row expectations (LOCKED — build to them).
Deliver: the opt-in generative text mode — for a registry text_target with mode:generative, candidates via ai_copy (offline default: deterministic seeded candidate synthesis, $0, no key needed — key only upgrades), EVERY candidate through the real copy Gate into a decision_pool cleared pool (structural Art II), workflow prebuilt (scripts/warm_copy_cache.py parallel to warm_hero_cache: generate+gate+cache offline, byte-identical replay) or realtime (cache-by-visit-key). Anthropic calls (if keyed) MUST be cost-logged via api_costs.record_anthropic_usage. tests/test_generative_text.py: planted lie/superlative/competitor/hold candidate 0-servable (real Gate), deterministic cache replay, $0 offline default, cost row on keyed path (monkeypatch transport).
