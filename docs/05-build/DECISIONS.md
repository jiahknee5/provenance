# Build decisions log

Numbered decisions made during the build. `basis=spec` = direct fact of the PRD/architecture;
`basis=interpretation` = a reasonable read of a gap. Architectural/strategic items were
surfaced at plan approval; PRD-silent items were logged here, not interrupted on.

| # | Decision | basis | Why |
|---|----------|-------|-----|
| R1 | Call it a **contextual bandit** (Thompson), never "RL" | interpretation | No state transitions/sequential credit; the anti-reward-hacking property is cleaner for a bandit and a judge would ding "RL." Architecture diagrams already say "bandit." |
| R2 | Prove "can't win by lying" with an **unconstrained twin** | interpretation | Graying out the lie shows exclusion, not proof. Same oracle/data, only the truth boundary differs → the twin converges to the lie, the constrained run can't. |
| R3 | **One** Assurance harness, sliced per channel | spec | "Same Gate, two channels" — two labs would imply two Gates and contradict P4. |
| R4 | The single LLM-judge exists **only as the Assurance baseline** | spec | The repo thesis is that single-LLM-judge fails >50% and is gameable; the Gate is NLI + calibrated ensemble + rules. |
| R5 | "Live" = **deterministic seeded replay**, not live inference | spec | The deck's own risk mitigation (pre-cache); extended from pages to the full run trace. |
| R6 | **Hybrid inference, deterministic default** | interpretation | No API key present + Python 3.14 ML-wheel risk → make NLI/judge/decomposer pluggable with deterministic offline defaults; auto-upgrade to DeBERTa/Claude/Ollama when present. The whole demo runs at $0 offline. |
| R7 | **Claim-level verdict cache** keyed (claim_id, source_version, rules_version) | interpretation | Structural guarantee the Gate runs per unique claim, never per user — keeps cost dollar-scale and makes drift surgical + warm-start real. |
| R8 | **Number-blind coverage** as the single-judge baseline | interpretation | A number-drifted claim is near-identical lexically; the baseline passes it, the Gate's numeric lens catches it — the honest reason the ensemble wins (P5). |
| R9 | **Stable hash** in the oracle (not Python `hash()`) | spec (Art. IV) | Builtin `hash()` is salted per process → the demo wasn't reproducible across runs. Fixed; guarded by a cross-process test. |
| R10 | Demo tenant = **Helix Analytics** (health-tech), hold on the 47% TCO claim | spec | Matches the showcase script (Helix Analytics / Northwind / 47% TCO). |
| R11 | Campaign-2 drift = **legal-hold flip** (not the source change) | interpretation | The source-change empties cfo (both variants cite the price); the hold flip pauses only the ROI variant so a verified arm survives — the clean transition. The source-change path is still proven (P3). |
| R12 | **Observatory** = adapt the /observe spine (append-only JSONL per lane, monotonic seq, poll-since-seq, honest empty state) to the *runtime* pipeline; instrument INSIDE the nodes; `emit()` no-op unless a recorder is attached | interpretation | Operator asked (2026-06-18) for observability + node graph w/ tooling + architecture diagram + logging + per-node input/decision/output evals. Mechanical logging at decision points = a claim can't be Gated without its trace; no-op-when-inactive keeps the 25 tests + determinism untouched. Logical clock (BASE_EPOCH+seq) keeps the ledger byte-identical. P1–P5 mirrored onto the dashboard, derived from the run's own artifacts (a signal that can go red); pytest stays authoritative. See PRD amendment A1. |
| R13 | **Enrichment = a provenance problem.** Every enriched fact is bound to its source connector + a lawful basis + a freshness TTL, passes an **Enrichment Gate** (usable/disclaimer/blocked), is dropped by Drift on expiry/retraction, and is audited by the Assurance Lab — mirroring the claim Gate. | interpretation | Operator's 2026-06-18 ask to personalize from other sources. Ungated facts-in-copy reintroduce the exact failure mode the product attacks (shipping the unsubstantiated). Gating facts makes the feature *strengthen* the thesis. See `docs/04-workflow/ENRICHMENT-TOUCHPOINTS.md`, PRD A2. |
| R14 | **Fork 1 = B (facts may appear in copy)**, but only Enrichment-Gate-`usable` facts with an on-file basis, audited by Assurance. | operator | Operator chose 1-B over the recommended 1-A on 2026-06-18 (more "wow"; accepts the compliance/creepiness exposure, mitigated by the fact-gate + audit). |
| R15 | **Fork 2 = B (one real free connector)**: live news-RSS (keyed on company) + DNS/MX on the email *domain*. $0, no PII to third parties, **opt-in via `PROVENANCE_ENRICH=live`** (default synthetic), responses **cached** (deterministic replay). Failures recorded honestly (blocked, reason="connector unavailable") — never a silent synthetic fallback. | operator | Operator chose 2-B over recommended 2-A on 2026-06-18. Network is a pre-authorized one-time action under BUILD-AUTONOMY only for free/no-PII reads; opt-in+cached preserves the 25 tests + the $0/offline/deterministic default. See `docs/audits/enrichment-privacy.md`. |
| R17 | **Education funnel = a new tenant on the engine** + a per-customer **system of record** with a **surface policy** (`say`/`allude`/`hold`). Operator ask 2026-06-19: model every funnel entry, collect by customer, personalize for both 1:1 outreach (creepiness-aware) and mass automation. | operator + interpretation | Operator chose "new tenant" + "build models + test suite". Surface policy makes "know it but don't mention it" auditable (derived from consent + source + sensitivity, per-channel); declared=say, behavioral/voice/rep-note=allude, sensitive/no-consent=hold. Class login = the website's magic-link token (one identity, whole funnel). `tests/test_funnel.py` (9 paths) proves the creepiness invariant: no `hold`/blocked fact reaches mass copy or a rep's `say`. See `docs/04-workflow/FUNNEL-AND-CUSTOMER-RECORD.md`. Built: `pipeline/customer/*`, `rules/academy_tenant.yaml`. UI rep-briefing page + live connectors are the next layer. |
| R18 | **Super-personalization demo = a provenance ledger, not a feature.** Operator ask 2026-06-19: show *all* data that could personalize a website, where each fact comes from (observed / declared / Google-OAuth / purchased-broker / first-party-CRM / identity-graph), synthesize values but label the source, escalate from an anonymous landing page → Google login → full append, and build a "super creepy" version showing everything possible. Built as `pipeline/personalization/*` + `/personalize` (tier × mode). | operator + interpretation | Reuses the funnel's **surface policy** (`say`/`allude`/`hold`) as the spine: "tasteful" honours it, "creepy" switches it off — so *creepy ≠ more data, creepy = no policy*, and every creepy line still carries its provenance (the repo thesis, applied to the personal layer). 52 signals across 7 tiers, each with a real vendor + acquisition method + honest cost ($0 observed → ~$0.05–0.25/record brokers) + creepiness 1–5. 100% synthetic single persona; no connector here calls a paid API (broker/identity rows documented, not invoked — mirrors `enrichment/catalog.py`). Deterministic (the "current time" is part of the persona — no wall clock/RNG). `tests/test_personalize.py` (10) proves: tier monotonicity, the Google-login jump is strictly-more OAuth, tasteful never surfaces a `hold` fact, creepy surfaces every available fact *with* provenance, creepy ⊇ tasteful, ledger disposition == policy decision. Text-only now; the same signals drive *layout* next. See `docs/04-workflow/PERSONALIZATION-PROVENANCE.md`. |
| R16 | **Golden evals + Assurance dashboard** (operator ask 2026-06-18). Every eval is a row **inputs → graph log → output → expected → pass**; the graph log is a nested subtable (`node · tool · input · decision/output`) captured via a new in-memory `observe` capture mode. Node-level steps (Library, Gate, Enrichment Gate) run **fresh with an isolated verdict cache** so every case computes its full graph log; workflow-level steps (Optimizer/Drift/Assurance/Website) are scored from the run's artifacts with graph logs pulled from the recorded ledger. One overarching **lifecycle eval** (Maria, end-to-end), same format. **Metrics over time** appended to `data/demo/eval_history.jsonl` (outside the observe ledger, so the byte-identical hash is preserved; wall-clock ts is the time axis). Served at **/assurance**; `tests/test_golden_evals.py` makes the suite a real gate (17/17). | interpretation | Capture-mode emit() is still a no-op without a recorder/capture, so the 34 prior tests + determinism are untouched. |
| R20 | **Real available data over synthetic personas** (operator ask 2026-06-20: "use real available data" + "why is maya chen there?"). Removed the named synthetic **"Maya Chen"** persona from `/google`; the top ("granted") tier is now built from **real, live-fetched** signals via new `pipeline/personalization/realenrich.py` — email-domain parse + personal/corporate classify (always, no network) + **Gravatar** public profile by email-hash + **company news** RSS for corporate domains (network, gated by the existing `PROVENANCE_ENRICH=live` opt-in). Each row is tagged `real=True` only when actually fetched; placeholders ("filled by a real Google sign-in") are `real=False` so the UI can't overclaim. Lower tiers reframed as **illustrative categories**, not one fabricated person's dossier. Added the operator's own record (`johnny.c.chung@gmail.com`) to the cohort so the flow is testable end-to-end, and a **per-person magic-link QR** on `/admin/landing/{id}` (the "just a QR, no login" path for the 40). | operator + interpretation | The project's thesis is *don't ship what you can't prove* — a named synthetic dossier violated that on the page that's literally about real data. Empirically, a bare personal Gmail yields **almost nothing free** (gmail → "personal mailbox", no Gravatar); rich data must come from the operator's own Vector/HubSpot/Clay (load an export), a real Google sign-in, or a paid broker — now stated honestly instead of papered over. **Clay**: verified no free-tier synchronous REST enrichment API (Enterprise + async/table-driven only) → recommended pre-enrich-in-table → export → load; the provided key was flagged for rotation and never committed/invoked. Network stays opt-in + cached, so the 72 tests + $0/offline/deterministic default are untouched. |
| R21 | **People Data Labs is the real person-enrichment provider** (operator ask 2026-06-20: "you have clay api, can't you enrich?" + "I want real data"). Probed the operator's Clay key against the live API: v1 enrich → 404 "deprecated", v3 → 401 "must be logged in as an admin" → Clay genuinely can't do API enrichment (table/webhook/async + Enterprise-only). Wired **PDL** (`/v5/person/enrich`, `X-Api-Key`, free ~100/mo) in `realenrich.py`; inert unless `PDL_API_KEY` set. Verified live: `sean@peopledatalabs.com` → real name/title/company/LinkedIn; personal Gmail → real `404 not_found`. Free tier redacts some PII to a bool flag → keep only real strings. `/google` shows a green "Actually fetched — REAL" panel when PDL returns, and an honest "no match" note otherwise. | operator + interpretation | The thesis forbids placeholders dressed as data; PDL is a real "email→person" API (what Clay isn't). Key in env only, never committed; rotate after demo. 72 tests unchanged (PDL inert by default). Real path for the cohort 40 = enrich their real emails via PDL (fits the 100/mo) or load a HubSpot/Clay CSV export. |
| R19 | **Cohort personalization demo** (operator ask 2026-06-19). QR → enter gauntletai email → match the cohort DB → a personalized landing (UI **and** content) + a personalized email + an admin gallery of 10 users with a "how this page was built" explanation. Core data = **HubSpot (real first-party) + LinkedIn (public)**; broker/identity-graph/deep-OAuth **greyed**; deep/creepy-tier values synthetic, keyed to email. | operator | Operator chose HubSpot-real + LinkedIn-public + synthetic-deep, and **~6 UI archetypes** over 10 bespoke pages. **8-family segmentation** (their behavioral/preferential/lifestage + added professional/intent/economic/channel/psychographic), each segment carries its **evidence** for the explanation. Segments → one of 6 archetypes (outcomes/cost/fast-track/explorer/welcome-back/prestige). **Context tier 1–4** reuses the surface policy (1=say-only … 4=policy off). `/lp` (QR+match), `/admin/landings`, `/admin/landing/{id}`. Built: `pipeline/personalization/{cohort,segments,landing}.py`; `tests/test_cohort.py` (8) proves all 6 archetypes hit, tier-1 say-only, tier-4 reveals creepy. Added `segno` for the QR. Live connectors (real HubSpot/LinkedIn) are the next layer. |
