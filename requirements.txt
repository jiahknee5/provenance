# Provenance demo — required (lightweight, runs fully offline on Python 3.11)
fastapi
uvicorn[standard]
jinja2
python-multipart
pydantic
numpy
pyyaml
httpx
rank-bm25
segno
pytest
anthropic   # optional LLM layer (Gate + /demo/live copy agent); inert without ANTHROPIC_API_KEY
